# 📦 项目依赖说明

## 核心依赖

### React 生态
```json
{
  "react": "^19.2.0",
  "react-dom": "^19.2.0",
  "react-router-dom": "^6.x"
}
```
- **react**: React 核心库
- **react-dom**: React DOM 渲染
- **react-router-dom**: 客户端路由管理

### UI 框架
```json
{
  "antd": "^5.x"
}
```
- **antd**: Ant Design UI 组件库
  - 提供丰富的企业级 UI 组件
  - 包含 Table、Form、Modal、Button 等
  - 内置中文国际化

### HTTP 客户端
```json
{
  "axios": "^1.x"
}
```
- **axios**: HTTP 请求库
  - 支持请求/响应拦截器
  - 自动转换 JSON 数据
  - 支持 Promise API

### 日期处理
```json
{
  "dayjs": "^1.x"
}
```
- **dayjs**: 轻量级日期库
  - Ant Design 推荐使用
  - 替代 moment.js
  - 支持国际化

### 图标库
```json
{
  "@ant-design/icons": "^5.x"
}
```
- **@ant-design/icons**: Ant Design 图标库
  - 包含大量矢量图标
  - 支持自定义颜色和大小

## 开发依赖

### TypeScript
```json
{
  "typescript": "~5.9.3",
  "@types/react": "^19.2.5",
  "@types/react-dom": "^19.2.3",
  "@types/node": "^24.10.1"
}
```
- 提供类型检查
- 增强代码提示
- 编译时错误检测

### 构建工具
```json
{
  "vite": "^7.2.4",
  "@vitejs/plugin-react": "^5.1.1"
}
```
- **vite**: 快速的前端构建工具
  - 极速的热重载
  - 开箱即用的 TypeScript 支持
  - 优化的生产构建

### 代码质量
```json
{
  "eslint": "^9.39.1",
  "typescript-eslint": "^8.46.4",
  "eslint-plugin-react-hooks": "^7.0.1",
  "eslint-plugin-react-refresh": "^0.4.24"
}
```
- **eslint**: 代码规范检查
- **typescript-eslint**: TypeScript 规则
- **eslint-plugin-react-hooks**: React Hooks 规则

## 依赖说明

### 为什么选择这些依赖？

#### 1. Ant Design
- ✅ 企业级 UI 设计规范
- ✅ 组件丰富且质量高
- ✅ 中文文档完善
- ✅ 活跃的社区支持
- ✅ 与 React 深度集成

#### 2. Axios
- ✅ 简洁的 API
- ✅ 强大的拦截器
- ✅ 支持请求取消
- ✅ 自动转换数据
- ✅ 浏览器和 Node.js 通用

#### 3. React Router v6
- ✅ React 官方推荐
- ✅ 声明式路由
- ✅ 嵌套路由支持
- ✅ 路由守卫功能
- ✅ 懒加载支持

#### 4. Vite
- ✅ 开发速度快
- ✅ HMR 体验好
- ✅ 配置简单
- ✅ 构建优化
- ✅ 插件生态丰富

## 安装命令

### 安装所有依赖
```bash
npm install
```

### 单独安装生产依赖
```bash
npm install --production
```

### 安装新依赖
```bash
# 生产依赖
npm install <package-name>

# 开发依赖
npm install -D <package-name>
```

## 版本兼容性

### Node.js 版本
- **要求**: >= 22.12 或 >= 20.19
- **推荐**: 22.x LTS

### 浏览器支持
- Chrome >= 90
- Firefox >= 88
- Safari >= 14
- Edge >= 90

## 依赖更新

### 检查过期依赖
```bash
npm outdated
```

### 更新依赖
```bash
# 更新所有依赖到最新兼容版本
npm update

# 更新特定依赖
npm update <package-name>
```

### 安全审计
```bash
# 检查安全漏洞
npm audit

# 自动修复
npm audit fix
```

## 打包大小优化

当前配置已做的优化：
- ✅ Vite 自动代码分割
- ✅ Tree Shaking 移除未使用代码
- ✅ 按需加载 Ant Design 组件
- ✅ 压缩 JavaScript 和 CSS

预估打包大小：
- **开发版本**: ~5MB（未压缩）
- **生产版本**: ~500KB（gzip 压缩后）

## 常见问题

### Q: 为什么 Ant Design 包这么大？
A: Ant Design 包含了所有组件，但 Vite 会自动进行 Tree Shaking，只打包使用到的组件。

### Q: 可以替换 Ant Design 吗？
A: 可以，但需要重写所有 UI 组件。推荐使用 Material-UI 或 Chakra UI。

### Q: 为什么不用 Redux？
A: 对于这个项目，React Context API 已经足够，Redux 会增加复杂度。

### Q: Axios 可以替换为 Fetch API 吗？
A: 可以，但需要自己实现拦截器、错误处理等功能。

## 许可证

所有依赖的许可证：
- React: MIT
- Ant Design: MIT
- Axios: MIT
- React Router: MIT
- Vite: MIT
- TypeScript: Apache-2.0

所有依赖都是开源且商业友好的。
